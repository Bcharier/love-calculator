import { PercetangePipe } from './percetange.pipe';

describe('PercetangePipe', () => {
  it('create an instance', () => {
    const pipe = new PercetangePipe();
    expect(pipe).toBeTruthy();
  });
});
